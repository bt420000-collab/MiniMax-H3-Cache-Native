# Examples

`H3BC_v2_node_fragment.json` records the alpha4 BALANCED node settings and intended MODEL wiring.

The package also contains `video_minimax_h3_i2v_H3BC_v2_alpha.json`, an alpha4 I2V test workflow. Treat it as a validation workflow rather than a production-quality benchmark until real GPU calibration is complete.

Validation order: OFF -> LOSSLESS / REFERENCE -> SAFE -> BALANCED -> AGGRESSIVE.
